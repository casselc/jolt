# Generic dispatch deliberately executed while a named logical mutex is held.
# Every row is issue-backed and count-exact; decreases are stale debt that must
# be reviewed, not silently retained. Safe named acquisition edges are computed
# separately and are never debt. The reviewed debt set is currently empty.
