{:doc
 "Conformance profile for test/chez/corpus.edn, generated by certify.clj. Each entry is a NON-portable case (keyed by [suite label]) and the host feature(s) it requires. Cases NOT listed are portable — they pass on any faithful Clojure. A runtime's conformance LEVEL = portable + the feature families it implements. See SPEC.md.",
 :clojure-version "1.12.5",
 :portable-count 5095,
 :non-portable-count 221,
 :feature-counts
 {:async/core-async 2,
  :concurrency/snapshot 1,
  :host/arrays 2,
  :host/implicit-imports 30,
  :host/jvm-interop 154,
  :impl/representation 2,
  :numerics/double-only 1,
  :perf/unbounded 1,
  :printer/jolt 2,
  :reader/jolt 25,
  :strictness/jolt 1},
 :entries
 [{:suite "*ns* / identity & printing",
   :label "tracks in-ns",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "async / close does not complete pending put",
   :label "pending rendezvous put parks until consumed, even on close",
   :bucket :uncertifiable,
   :features [:async/core-async]}
  {:suite "async / transducer backpressure",
   :label "fixed-buffer transducer put blocks when full",
   :bucket :uncertifiable,
   :features [:async/core-async]}
  {:suite "chunked-seq / over a vector",
   :label "chunk-first window past the first block",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "chunking-model / unchunked realization granularity",
   :label "dedupe is fully lazy at construction",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "chunking-model / unchunked realization granularity",
   :label "mapcat is fully lazy at construction",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "clojure.core / leaf batch (complement fnil munge etc.)",
   :label "supers empty set",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "clojure.math",
   :label "sqrt",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "clojure.string / replace",
   :label "char match, string replacement",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "concurrency-model / compare-and-set!",
   :label "immediate fixnums have no distinct boxes",
   :bucket :divergent,
   :features [:concurrency/snapshot]}
  {:suite
   "conformance / defmacro surface + syntax-quote/ns (enabling real clojure libs)",
   :label "syntax-quote resolves alias",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite
   "conformance / migratus enablement: def/defmacro/defmulti forms, assoc, try, ns",
   :label "ns restored after catch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite
   "conformance / migratus enablement: def/defmacro/defmulti forms, assoc, try, ns",
   :label "try finally on ok+catch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite
   "conformance / migratus enablement: def/defmacro/defmulti forms, assoc, try, ns",
   :label "try multi-body last",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "conformance / numeric pass",
   :label
   "literal-init loop var + bignum-literal stays generic (exact)",
   :bucket :jvm-raises,
   :features [:numerics/double-only]}
  {:suite "conformance / try / catch",
   :label "ex-data",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "conformance / try / catch",
   :label "ex-message",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "conformance / try / catch",
   :label "try catch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "core / extenders",
   :label "extends? on inline defrecord impl",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "core / with-open",
   :label "binding visible",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "core / with-open",
   :label "body result",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "core / with-open",
   :label "close runs",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "core / with-open",
   :label "nested close order",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "core / with-precision",
   :label "rounding arg accepted",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "deftype / IPersistentStack",
   :label "peek/pop dispatch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "deftype / map-like dispatch",
   :label "dissoc + keys via IPersistentMap/Seqable methods",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label "a default sorted set is unaffected",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label
   "a sorted set with an =-inconsistent comparator still dedups",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label
   "an empty set answers () like an empty vector, not seq's nil",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label "distinct over a map yields its entries",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label "distinct over a set keeps every element",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "distinct / any seqable",
   :label "the head of a distinct set is an element, not nil",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "exceptions / ex-info",
   :label "catch binds thrown value",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / ex-info",
   :label "ex-data via catch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / ex-info",
   :label "propagates to outer",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / ex-info",
   :label "rethrow preserves ex",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / try-catch",
   :label "catch :default",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / try-catch",
   :label "catch binds error",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / try-catch",
   :label "catch value of body",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / try-catch",
   :label "finally runs on throw",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "exceptions / try-catch",
   :label "no throw -> body",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "forms / fn",
   :label "no param vector",
   :bucket :throws-mismatch,
   :features [:strictness/jolt]}
  {:suite "forms / try",
   :label "catch ex-data",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "forms / try",
   :label "catch value w/ finally",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "forms / try",
   :label "first throw wins",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "forms / try",
   :label "immediate throw caught",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "forms / try",
   :label "no throw skips catch",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / .-field reads a declared field",
   :label "a key assoc'd onto a record is not a field",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / .-field reads a declared field",
   :label "a slot the record does not declare",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / UUID + Long + shiftLeft",
   :label "(UUID. msb lsb), .shiftLeft, (Long. n)",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "host-interop / class tokens & readers",
   :label "dispatch-only class name",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / class tokens & readers",
   :label "slurp accepts :encoding opts",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / class tokens & readers",
   :label "slurp drains a StringReader",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / exception + HashMap shims",
   :label "HashMap get",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / exception + HashMap shims",
   :label "HashMap put + size",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / logging host shims",
   :label "pprint respects *print-length*",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / migratus class shims",
   :label "Timestamp is millis",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "host-interop / reader-feature toggle",
   :label "features default to jolt+default",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / reader-feature toggle",
   :label "restore returns to default",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / reader-feature toggle",
   :label "set + read back",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "host-interop / ring-codec surface",
   :label "Base64 encode",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "Base64 round trip",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "MapEntry key/val",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "StringTokenizer",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "URLDecoder www form",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "URLEncoder www form",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "extend-protocol Map",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "extend-protocol Map covers sorted",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "host-interop / ring-codec surface",
   :label "url round trip",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "inst / partial timestamps & offsets",
   :label "bad timestamp throws",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "bigint of a small value collapses",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "byte class",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "float class collapses to Double",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "instance? Byte is false",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "instance? Float is true",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "instance? Long is true",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "int class",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "integer-box-model / narrow int class collapses to Long",
   :label "short class",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "interop / PushbackReader & parse statics",
   :label "edn/read multi-line",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "interop / PushbackReader & parse statics",
   :label "unread accepts a char",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "interop / PushbackReader & parse statics",
   :label "unread pushes back",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "interop / String methods",
   :label "String/valueOf nil",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "interop / StringReader & StringBuilder",
   :label "mark/reset",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "interop / StringReader & StringBuilder",
   :label "with-open closes shim",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "interop / class model",
   :label
   "class? is true for a Class and for a record type token (jolt unifies a record with its ->ctor)",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "interop / dot forms",
   :label "dot field keyword",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "interop / dot forms",
   :label "field access .-",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "interop / dot forms",
   :label "method call",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "interop / dot forms",
   :label "method with args",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "io / cold tagged types via print-method",
   :label "atom override fires nested",
   :bucket :divergent,
   :features [:reader/jolt]}
  {:suite "io / cold tagged types via print-method",
   :label "transient map",
   :bucket :divergent,
   :features [:printer/jolt]}
  {:suite "io / cold tagged types via print-method",
   :label "transient vector",
   :bucket :divergent,
   :features [:printer/jolt]}
  {:suite "io / line-seq",
   :label "line-seq",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "io / line-seq",
   :label "line-seq count",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "io / line-seq",
   :label "line-seq empty",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "io / line-seq",
   :label "line-seq is lazy seq",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "io / print-method multimethod",
   :label "StringWriter accumulates",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "io / print-method multimethod",
   :label "a direct call renders a regex like pr",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "io / print-method multimethod",
   :label "a direct call renders a uuid like pr",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "io / print-method multimethod",
   :label "direct builtin override",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "io / print-method multimethod",
   :label "direct call uses :default",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "io / print-method multimethod",
   :label "print-dup routes to print-method",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "io / slurp, spit, printf, flush (host-classified)",
   :label "file-seq finds files",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "macros / core-overlay",
   :label "assert throws",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "macros / core-overlay",
   :label "condp no match",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "macros / time, with-redefs, macroexpand",
   :label "with-redefs restores on throw",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "macros / vswap! divergence",
   :label "vswap! is a macro on JVM, fn on jolt",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "metadata / type hints",
   :label "type hint in let",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "multimethods / hierarchies",
   :label "explicit :hierarchy",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "namespaces / ns operations",
   :label "in-ns switches",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "namespaces / ns operations",
   :label "standalone use",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "namespaces / unified alias store",
   :label "alias fn visible to ns-aliases",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "namespaces / unified alias store",
   :label "ns-unalias removes both views",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "namespaces / unified alias store",
   :label "require :as registers the alias",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "numbers / unsigned string conversions",
   :label "a negative long takes the narrowest width holding it",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "numbers / unsigned string conversions",
   :label "past 64 bits it is a signed magnitude, not a wrap",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "permissive / empty MapEntry",
   :label "empty of a map entry",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "permissive / keys on pair vector",
   :label "keys works on vector of pairs",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "predicates / overlay-migrated",
   :label "keyword-identical?",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "predicates / overlay-migrated",
   :label "keyword-identical? no",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "predicates / overlay-migrated",
   :label "object?",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "predicates / overlay-migrated",
   :label "undefined?",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "predicates / tagged-value",
   :label "atom? no",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "predicates / tagged-value",
   :label "atom? yes",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "protocols / satisfies?",
   :label "satisfies? on a class or interface answers instance?",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "reader / digit separators",
   :label "a bigint suffix follows a grouped literal",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "a radix literal groups its digits",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "a ratio groups either side",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "a run of underscores is one separator",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "a separator survives a leading sign",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "both sides of a decimal point group",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "hex groups by nibble",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "mantissa and exponent group independently",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "octal keeps its leading zero",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / digit separators",
   :label "underscores group a long decimal",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / duplicate-literal",
   :label "duplicate keyword element",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / duplicate-literal",
   :label "duplicate map key",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / duplicate-literal",
   :label "duplicate set element",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / duplicate-literal",
   :label "duplicate set element by value, not identity",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / duplicate-literal",
   :label "equal quoted lists are one element",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "reader / nested syntax-quote",
   :label "a quoted template is that same construction code",
   :bucket :divergent,
   :features [:impl/representation]}
  {:suite "reader / nested syntax-quote",
   :label
   "the inner template is jolt's construction code, not the JVM's seq/concat",
   :bucket :divergent,
   :features [:impl/representation]}
  {:suite "reader / radix N suffix",
   :label "2r1010N reads as integer 10",
   :bucket :jvm-raises,
   :features [:reader/jolt]}
  {:suite "reader / surrogate in string",
   :label "\\ud800 in string throws Invalid character constant",
   :bucket :throws-mismatch,
   :features [:host/jvm-interop]}
  {:suite "reader-model / nested fn literals",
   :label "nested #() reads",
   :bucket :jvm-raises,
   :features [:reader/jolt]}
  {:suite "records / literal data",
   :label "a data list of strings is a list, not a call",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "records / literal data",
   :label "a list value is data, not code",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "records / literal data",
   :label "a symbol value is the symbol, not its binding",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "records / literal data",
   :label "nested record literal still constructs",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "refs / loaded-libs",
   :label "*loaded-libs* is a ref",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "refs / loaded-libs-dosync",
   :label "*loaded-libs* can be altered inside dosync",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "refs / ref",
   :label "ref creates a ref",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "Pattern/MULTILINE ^",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "Pattern/compile is a regex",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "Pattern/quote literal",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "Pattern/quote not meta",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "compiled .split",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "regex / Pattern statics & String regex methods",
   :label "str/replace w/ Pattern",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "regex / \\p property classes",
   :label "unknown class throws",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "regex / literals & predicate",
   :label "regex? literal",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "regex / literals & predicate",
   :label "regex? non-regex",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "regex / re-pattern & string ops",
   :label "re-pattern is regex?",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "seq-type-model / remaining class collapse",
   :label "cycle",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "seq-type-model / remaining class collapse",
   :label "drop over a vector",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "seq-type-model / remaining class collapse",
   :label "repeat",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "seq-type-model / specialized seq classes",
   :label "bigint of a small bound is a long here",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "set / nil element",
   :label "transient conj! nil",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "set / nil element",
   :label "transient contains? nil",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "set / nil element",
   :label "transient disj! nil cnt",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "set / nil element",
   :label "transient disj! removes",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "sorted / empty + empty? + rseq + printing",
   :label "empty set kind",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "sorted / predicates",
   :label "sorted-map? false",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "sorted / predicates",
   :label "sorted-map? true",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "sorted / predicates",
   :label "sorted-set? false",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "sorted / predicates",
   :label "sorted-set? true",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "state / atoms",
   :label "atom? on non-atom",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "state / atoms",
   :label "atom? predicate",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "state / promises",
   :label "promise undelivered",
   :bucket :timeout,
   :features [:perf/unbounded]}
  {:suite "streams / pipes and stream targets",
   :label "spit writes into a writer",
   :bucket :certified,
   :features [:host/implicit-imports]}
  {:suite "string-model / codepoint strings",
   :label "charAt of an astral character is the codepoint",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "string-model / codepoint strings",
   :label "count of astral string",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "string-model / codepoint strings",
   :label "seq counts codepoints",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "string-model / codepoint strings",
   :label "subs across astral boundary",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "tagged literals / value equality",
   :label "duplicate literal keys throw at read (same form twice)",
   :bucket :read-error,
   :features [:reader/jolt]}
  {:suite "time / sdf locale names",
   :label "explicit ROOT locale id renders ROOT names",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "time / sdf locale names",
   :label "no-locale name directives render ROOT names",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "time / sdf locale names",
   :label "numeric pattern ignores the locale id",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "time / sdf locale names",
   :label "unregistered locale id falls back to ROOT",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "transient / set",
   :label "conj! dedups",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "transient / set",
   :label "map elements by value",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "transient / vector",
   :label "conj! many args",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "transient / vector",
   :label "transient? false",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "transient / vector",
   :label "transient? true",
   :bucket :uncertifiable,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "array-seq",
   :bucket :uncertifiable,
   :features [:host/arrays]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "array-seq empty",
   :bucket :uncertifiable,
   :features [:host/arrays]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "bean is the map",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "enumeration-seq",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "init-proxy",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "iterator-seq",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "proxy-call-with-super calls",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "proxy-mappings",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / JVM-shape stubs (documented jolt behavior)",
   :label "update-proxy",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-boolean",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-byte",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-char",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-double",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-float",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-int",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-long",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / array stubs (vectors + host buffers)",
   :label "aset-short",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / chunk family (eager equivalents) + cat",
   :label "chunk round-trip",
   :bucket :divergent,
   :features [:host/jvm-interop]}
  {:suite "untested / protocols: extend + extends?",
   :label "extend registers",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / protocols: extend + extends?",
   :label "extend two methods",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / protocols: extend + extends?",
   :label "extends? after extend",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / protocols: extend + extends?",
   :label "extends? without",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "booleans",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "bytes view",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "chars first",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "ints",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "longs",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}
  {:suite "untested / typed coercion views",
   :label "shorts",
   :bucket :jvm-raises,
   :features [:host/jvm-interop]}]}
